import React from 'react'
import Styles from '../Style'

const Section = () => {
  return (
    <section className={`container ${Styles.padding} ${Styles.text}`}>
        <div>
            Section
        </div>
    </section>
  )
}

export default Section